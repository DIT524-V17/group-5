/**
 * Created by Vin on 18/05/2017.
 */
public class ParsedDijkstra {
    int length;
    double degrees;
    boolean direction; //1 = Left;  0 = Right

    public ParsedDijkstra(Edge e1, Edge e2){
        //TODO: determine which node to assess
        //assume e1 is the edge the car is moving from and e2 is the edge we're returning values from
        //TODO: method to start the first edge with (node1 node2)
        //same method can be used to end the route given that the final point isnt a pre-defined node
    }
}
